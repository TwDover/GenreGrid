# Mined statistical priors

Per-genre JSON produced by `scripts/mine_corpus.py` from a real MIDI corpus.
Loaded by `app.services.priors`. Not committed by default — regenerate from your
own dataset (e.g. POP909, a genre-tagged Lakh subset).
