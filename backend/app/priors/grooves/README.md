# Mined drum-groove priors

Per-genre JSON from `scripts/mine_grooves.py` (Groove MIDI Dataset).
Each holds per-voice 16-step hit probabilities plus `derived` fields
(kick_pattern, snare_standard_beats, hat_density, swing) overlaid onto a
style's drums config at generation time.
